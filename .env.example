# Offline Sync Engine - Environment Variables Example

# Server Configuration
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# CORS Configuration
# Use '*' for development, specify origins for production
# Multiple origins can be comma-separated: https://app1.example.com,https://app2.example.com
CORS_ORIGIN=*
# Set to 'true' to allow credentials (cookies, authorization headers)
CORS_CREDENTIALS=false

# CouchDB Configuration
COUCHDB_URL=http://localhost:5984
COUCHDB_USERNAME=admin
COUCHDB_PASSWORD=password
COUCHDB_DB_PREFIX=offline-sync

# TUS Upload Configuration
TUS_UPLOAD_DIR=/tmp/tus-uploads
